<h2>Dashboard</h2>
<table>
  <tr>
  	<th>Title</th>
  	<th>Comments</th>
  	<th>Published</th>
  </tr>
  <% @posts.each do |post| %>
    <tr>
      <td><%= link_to post.title, post %></td>
      <td><%= post.comments.count %></td>
      <td><%= post.published %></td>
    </tr>
  <% end %>
</table>
