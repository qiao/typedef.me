(function($) {
  $comment = $("<%= escape_javascript(render(@comment)) %>").hide();
  $("#comments_list_<%= @commentable.id %>").append($comment);
  $comment.slideDown();
  $("#new_comment_<%= @commentable.id %>")[0].reset();
})(jQuery);
